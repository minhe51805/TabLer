# Build output / dependencies
dist
build
node_modules
**/node_modules/**

# Rust build artifacts
src-tauri/target

# Next.js website build output
website/.next
website/out

# Generated plugin repository artifacts
website/public/plugins

# Generated plugin manifests + registry — byte-exact contract with
# build-plugin-registry.mjs (JSON.stringify(…, null, 2)) which the CI
# `git diff --exit-code` determinism gate validates. Prettier collapses
# short arrays to one line; the generator always expands them.
plugins/*/plugin.json
plugin-registry.json

# Lockfiles and generated data
package-lock.json
pnpm-lock.yaml
yarn.lock
*.min.js
*.min.css

# Rust is handled by rustfmt, not prettier
*.rs

# Generated capability matrix — byte-exact contract with serde_json::to_string_pretty
# (test committed_json_matrix_matches_the_rust_catalog). Prettier collapses
# single-element arrays to one line; serde always expands them.
docs/generated/driver-capabilities.json
