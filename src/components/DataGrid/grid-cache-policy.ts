/**
 * Capacity bounds for the module-level data caches owned by `useDataGrid`.
 * Keeping the policy numbers beside a named export documents intent and
 * makes them tunable in one place.
 */

export const MAX_TABLE_PAGE_CACHE_ENTRIES = 48;
export const MAX_TABLE_COUNT_CACHE_ENTRIES = 24;
export const MAX_INLINE_STRUCTURE_CACHE_ENTRIES = 48;
